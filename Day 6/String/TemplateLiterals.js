// Template literals are strings written using backticks (``).
// They allow embedding variables and expressions using ${}.

let a = 10
let n = `hello world ${1+5+1} this is demo of\ntemplate literals ${a}` //you can write like these variable with string in template literals without writing separately
console.log(n)