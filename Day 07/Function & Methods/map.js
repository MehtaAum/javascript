//Using map to create a new array by returning each value from the original array.
//it is similar to foreach but foreach for use to some do any task and map used to create new array based on its return value 

let arr = [10,20,30,40,50]

let a = arr.map((val)=>{
    return val
})
console.log(a)
